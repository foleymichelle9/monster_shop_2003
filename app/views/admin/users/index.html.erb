<h1>All Users</h1>

<% @users.each do |user| %>
  <%= link_to  "#{user.name}", "admin/users/#{user.id}" %><br/>
  <p>Registered on: <%= user.created_at.strftime("%m-%d-%y") %></p><br/>
  <p>Type of user: <%= user.role %></p>
<% end %>